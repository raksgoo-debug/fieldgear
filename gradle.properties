org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
org.gradle.parallel=true

# --- Minecraft / Forge -------------------------------------------------------
minecraft_version=1.20.1
minecraft_version_range=[1.20.1,1.21)
forge_version=47.3.0
forge_version_range=[47,)
loader_version_range=[47,)
mapping_channel=official
mapping_version=1.20.1

# --- Mod ---------------------------------------------------------------------
# Renaming the mod: change mod_id here, rename the package folders under
# src/main/java, the assets/data folders under src/main/resources, and the
# MODID constant in FieldGear.java. Everything else reads from those.
mod_id=fieldgear
mod_name=Field Gear
mod_license=MIT
mod_version=1.1.0
mod_group_id=com.fieldgear
mod_authors=Raks
mod_description=Faceted 3D armour with insertable ballistic plates, mountable night vision and flip-up visors.

# --- Dependencies ------------------------------------------------------------
geckolib_version=4.8.2
